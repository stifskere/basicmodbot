module.exports = {
    name: "level",
    execute(message, args, config, moment, RichEmbed, date, embeds, bot, db){
       const member = message.author;
        const levelembed = new RichEmbed()
            .setColor(config.Embedcolor)
            .setTitle(`Your level is:`)

    }
}